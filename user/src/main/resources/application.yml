spring:
  application:
    name: rms-user
  main:
    web-application-type: reactive
  cloud:
    zookeeper:
      connect-string: test:12181
      discovery:
        root: /rms
  r2dbc:
    url: r2dbc:postgresql://172.19.216.212:5432/rms?schema=public
    username: postgres
    password: snubi1004
    pool:
      initial-size: 100
      max-size: 500
      max-idle-time: 30m
      validation-query: SELECT 1

logging:
  level:
    io.r2dbc.postgresql.QUERY: TRACE
#    io.r2dbc.postgresql.PARAM: TRACE
server:
  port: 9495

