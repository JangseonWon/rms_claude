kind: ConfigMap
apiVersion: v1
metadata:
  name: api
  namespace: {{ .Release.Namespace }}
data:
  application.yaml: |
    spring:
      application:
        name: rms-api
      config:
        import:
          - classpath:database.yaml
          - classpath:alis.yaml
      main:
        web-application-type: reactive
    server:
      port: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  annotations:
    alpha.image.policy.openshift.io/resolve-names: '*'
    app.openshift.io/route-disabled: 'false'
    app.openshift.io/vcs-uri: 'https://github.com/GC-Genome/rms.git'
    reloader.stakater.com/auto: 'true'
  name: api
  namespace: {{ .Release.Namespace }}
  labels:
    app: api
    app.openshift.io/runtime: spring-boot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
      annotations:
        sidecar.istio.io/inject: 'true'
    spec:
      volumes:
        - name: config
          configMap:
            name: api
            defaultMode: 420
        - name: database-config
          configMap:
            name: database
            defaultMode: 420
        - name: alis-config
          configMap:
            name: alis
            defaultMode: 420
      containers:
        - resources: {}
          terminationMessagePath: /dev/termination-log
          name: api
          env:
            - name: database.url
              valueFrom:
                secretKeyRef:
                  name: database
                  key: url
            - name: database.username
              valueFrom:
                secretKeyRef:
                  name: database
                  key: username
            - name: database.password
              valueFrom:
                secretKeyRef:
                  name: database
                  key: password
            - name: alis.private-exponent
              valueFrom:
                secretKeyRef:
                  name: alis
                  key: private-exponent
            - name: alis.private-modulus
              valueFrom:
                secretKeyRef:
                  name: alis
                  key: private-modulus
            - name: TZ
              value: Asia/Seoul
          ports:
            - containerPort: 8080
              protocol: TCP
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: config
              mountPath: /app/resources/application.yaml
              subPath: application.yaml
            - name: database-config
              mountPath: /app/resources/database.yaml
              subPath: database.yaml
            - name: alis-config
              mountPath: /app/resources/alis.yaml
              subPath: alis.yaml
          terminationMessagePolicy: File
          image: 'image-registry.openshift-image-registry.svc:5000/{{ .Release.Namespace }}/api:latest'
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 2
  progressDeadlineSeconds: 600
---
kind: Service
apiVersion: v1
metadata:
  name: rms-api
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - name: 80-tcp
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: api
---
kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: api
  namespace: {{ .Release.Namespace }}
spec:
  lookupPolicy:
    local: false
